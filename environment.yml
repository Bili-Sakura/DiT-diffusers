name: DiT
channels:
  - pytorch
  - nvidia
dependencies:
  - python >= 3.10
  - pytorch >= 2.0
  - torchvision
  - pytorch-cuda=11.8
  - pip:
    - -e .
    - accelerate
    - safetensors
    - pytest
